# vuex-first-steps

> A Vue.js project

## Build Setup

``` 
1. Instalar dependencias
npm install

2. Iniciar servidor (pararse en la carpeta "websokets" del proyecto)
node index.js

3. Iniciar servidor con auto-recarga en localhost:8080
npm run start

4. Instalar plugin "Allow-Control-Allow-Origin" para poder realizar las peticiones a la API.

   ¡IMPORTANTE! Desactivar el plugin para que funcione el servidor de WebSockets.
